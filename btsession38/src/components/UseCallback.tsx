import React from 'react'

export default function UseCallback() {
  return (
    <div>UseCallback
      {/* 
        cách dùng
        import useCallback nhận vào 2 đối số 
        1. callback là arowfunction ==> sử lý logic
        2. [] || [dependency] ==> khi dependence được gọi thì useCallback được gọi tương tự useEffect
        3. 
       */}
    </div>
  )
}
